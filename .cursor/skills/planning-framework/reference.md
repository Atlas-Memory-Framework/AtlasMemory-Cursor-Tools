# Planning Framework (legacy)

This skill is deprecated in favor of the `/plan` workflow.

Use:
- `.cursor/skills/plan/` for the orchestrator
- `.cursor/skills/plan/reference.md` for the SSOT template
